{
  "framework": "vite",
  "buildCommand": "./node_modules/.bin/tsc && ./node_modules/.bin/vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install --production=false",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}